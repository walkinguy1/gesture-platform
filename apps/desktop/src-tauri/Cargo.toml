[package]
name = "gesture-platform-desktop"
version = "1.0.0"
description = "Gesture Platform - ASL Translator desktop shell"
edition = "2021"
rust-version = "1.77"

[lib]
name = "gesture_platform_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
